# Read-later + in-app browser

**Prompt (copy-paste into Vibetree Pro)**

```
Build a read-later app. List of saved URLs; tap one to open in an in-app WKWebView with a back button. Add new URL (title + URL). Persist in UserDefaults.
```
