# Simple counter

**Prompt (copy-paste into Vibetree Pro)**

```
Build a simple counter app. One screen: large number and a + button. Persist the count in UserDefaults so it survives restarts. Clean, minimal.
```
