# Cursor Rules

**For system-improvement or cloud agents:** See **`AGENTS.md`** in the repo root (do not build apps; use build-results and code edits only).

Quick reference — rules live in `rules/` with numbered prefixes:

| File | What it covers |
|------|----------------|
| `01-workflow-automate-dev-actions.mdc` | Restart dev server yourself, push to GitHub yourself |
| `02-ui-theme-and-dropdowns.mdc` | Twilight Violet theme, DropdownSelect, design tokens |

Adding new rules: use `03-`, `04-`, etc. and keep `.mdc` extension.

**Prompt/skills plan:** See **`docs/System_Prompt_Restructuring_Plan.md`** for the plan to slim the Swift system prompt and move integration rules into conditional skills.
