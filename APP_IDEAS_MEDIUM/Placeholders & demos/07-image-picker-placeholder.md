# Image picker placeholder (Medium)

**Prompt (copy-paste into Vibetree Pro)**

```
Build a photo album app placeholder. Main screen: grid of photo placeholder cards (colored rectangles with numbers). Tap photo for full-screen view with share and delete buttons. Add photo button shows mock picker (3 options: Camera, Library, Color). Picking creates a new colored placeholder. Albums tab: create/manage albums, assign photos. Slideshow mode: auto-advance through photos. Persist with Codable + UserDefaults.
```
