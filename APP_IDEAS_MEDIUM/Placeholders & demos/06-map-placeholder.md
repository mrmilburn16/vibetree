# Map placeholder (Medium)

**Prompt (copy-paste into Vibetree Pro)**

```
Build a places app with map. Main screen: Map view with 5 annotated pins at different coordinates. Tap pin to show callout with place name and category. List view toggle: same places as a list with distance placeholder. Add place sheet: name, category picker, coordinate fields (or use map tap). Detail screen: map zoomed to pin, place info, and notes field. Filter pins by category. Persist places with Codable + UserDefaults.
```
