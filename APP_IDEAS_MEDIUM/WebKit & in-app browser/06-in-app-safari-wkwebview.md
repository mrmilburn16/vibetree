# In-app Safari / WKWebView (Medium)

**Prompt (copy-paste into Vibetree Pro)**

```
Build a mini browser. Main screen: URL bar at top, WKWebView below, toolbar at bottom with back, forward, refresh, and share buttons. Bookmarks sheet: list of saved bookmarks; tap to load. Add current page to bookmarks button. Persist bookmarks with Codable + UserDefaults. Show loading progress bar while page loads.
```
