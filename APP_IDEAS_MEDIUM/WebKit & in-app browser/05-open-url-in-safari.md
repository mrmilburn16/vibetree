# Open URL in Safari (Medium)

**Prompt (copy-paste into Vibetree Pro)**

```
Build a URL launcher with history. Main screen: text field for URL, Open button that launches in Safari, and a history list of previously opened URLs with timestamps. Tap history item to re-open. Swipe to delete history entries. Validate URL format before opening. Persist history with Codable + UserDefaults. Show empty state when no history.
```
