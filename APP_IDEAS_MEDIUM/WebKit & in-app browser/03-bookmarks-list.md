# Bookmarks list (Medium)

**Prompt (copy-paste into Vibetree Pro)**

```
Build a bookmarks manager with folders. Main screen: list of folders; tap folder to see bookmarks inside. Each bookmark has title, URL, and optional note. Add/edit bookmark sheet with folder picker. Swipe to move between folders. Search across all bookmarks. Long-press to copy URL. Persist folder tree with Codable + UserDefaults. Empty state when no bookmarks.
```
