# Dynamic Type (Medium)

**Prompt (copy-paste into Vibetree Pro)**

```
Build an accessibility showcase. Main screen: article-style content with headline, subheadline, body, caption, and footnote — all scaling with Dynamic Type. Accessibility inspector panel (toggle): shows current text size category, contrast ratio indicator. Custom font scaling for non-standard elements. Preview at different sizes (slider). Bold Text support. Reduce Motion support. Persist any preferences with UserDefaults.
```
