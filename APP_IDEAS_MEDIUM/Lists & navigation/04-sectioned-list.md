# Sectioned list (Medium)

**Prompt (copy-paste into Vibetree Pro)**

```
Build a sectioned contact directory. Main screen: alphabetically sectioned list with section index (A–Z sidebar). Each row shows avatar initial, name, and role. Tap for detail screen: full info with action buttons. Search bar that searches across all sections. Add person sheet with name, role, and email. Section headers show count. Persist with Codable + UserDefaults.
```
