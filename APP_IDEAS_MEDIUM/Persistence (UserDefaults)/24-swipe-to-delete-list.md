# Swipe-to-delete list (Medium)

**Prompt (copy-paste into Vibetree Pro)**

```
Build an editable list with multiple actions. Main screen: list of items with swipe-left to delete (with confirmation) and swipe-right to favorite. Favorites section at top. Add item sheet with name and optional emoji icon. Reorder items with drag handles (EditButton). Undo last delete with toast. Show item count in navigation title. Persist with Codable + UserDefaults.
```
