# Quick contacts (Medium)

**Prompt (copy-paste into Vibetree Pro)**

```
Build a contacts app with groups. Main screen: alphabetically sectioned list of contacts (name, phone, email). Tap for detail screen showing all info with call/email action buttons (show alert). Add/edit contact sheet with validation (require name). Groups tab: create groups and assign contacts. Search bar. Swipe to delete. Persist with Codable + UserDefaults.
```
