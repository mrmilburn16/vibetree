# Empty state (Medium)

**Prompt (copy-paste into Vibetree Pro)**

```
Build an app with contextual empty states. Main screen: tab bar with 3 tabs. Each tab shows a different empty state: Tab 1 (inbox icon + 'No messages yet' + 'Start a conversation' button), Tab 2 (search icon + 'No results' + search suggestion chips), Tab 3 (star icon + 'No favorites' + 'Browse items' button). Tapping CTA buttons shows sample content. Animated icon on each empty state.
```
