# Lap timer (Medium)

**Prompt (copy-paste into Vibetree Pro)**

```
Build a multi-athlete lap timer. Main screen: list of athletes/lanes (add/remove). Tap an athlete to record a lap — shows lap times in a sub-list. Global start/stop that starts all timers simultaneously. Summary view: table comparing athletes' lap times side by side, with fastest lap highlighted. Reset all button with confirmation. Persist with Codable + UserDefaults.
```
