# Gradient background (Medium)

**Prompt (copy-paste into Vibetree Pro)**

```
Build a gradient design tool. Main screen: full-screen gradient preview with two color pickers (start and end colors). Direction picker: horizontal, vertical, diagonal, radial. Presets row: 8 preset gradient combinations (Sunset, Ocean, Forest, etc.). Save gradient to collection. Collection tab: grid of saved gradients. Tap to apply. Export gradient as CSS code (share sheet). Animated gradient transitions when changing. Persist with Codable + UserDefaults.
```
