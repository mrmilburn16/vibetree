# 7. Mood / journal with smooth animations

**Showcases:** `withAnimation`, list transitions, state updates.

**MVP:** Picker for mood (e.g. 5 emoji), optional note, Save. List of past entries with animation when adding.

---

## Prompt (copy-paste into Vibetree Pro)

```
Build a mood journal. One screen: a horizontal picker or row of 5 emoji mood options (e.g. 😢 😐 🙂 😊 🎉). Below that an optional short note TextField and a Save button. When the user saves, add the entry to a list below (show mood emoji, note if any, and date). Use withAnimation when appending to the list so new entries animate in. Show the list in reverse order (newest first).
```
